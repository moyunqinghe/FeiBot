---
name: echo
description: 示例 skill:演示 skills/ 目录的存放格式,不含实际能力。
---

# echo

这是一个示例 skill,用于验证 skill 发现机制(agent/skills/loader.py 只列名字,不执行)。

后续在这里放真实 skill:每个子目录一个 SKILL.md,frontmatter 写 name/description,
正文写触发时机与操作步骤。
